workflows:
  ios-release:
    name: iOS Release
    max_build_duration: 60
    instance_type: mac_mini_m2
    environment:
      ios_signing:
        distribution_type: app_store
        bundle_identifier: com.betsports.fantasy
    scripts:
      - name: Build
        script: echo "Build configured"
    artifacts:
      - build/ios/ipa/*.ipa
